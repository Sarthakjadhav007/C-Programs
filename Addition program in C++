#include <iostream>
using namespace std;  

int main() {
    float no1, no2, sum;
    cout << "Enter first number: ";
    cin >> no1;
    cout << "Enter second number: ";
    cin >> no2;
    sum = no1 + no2;
    cout << "The sum of addition is: " << sum << endl;
    return 0;
    }
